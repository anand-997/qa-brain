# Fetch the JIRA ID/Context and Create a Test Plan Generator
# TICKET-ID-101 -> Fetch Test Strategy

You please read the file of ABOUT_B.L.A.S.T.md to understand only about blast framework, and create a test cases generator agent with React application which will take:
- the Jira/zoho sprint configuration
- Jira/zoho email ID
- Jira/zoho token
- my Jira/zoho sprint base URL
- GROQ connection API details in the settings and take the JIRA ID/Plain text/.txt/.md as requirement doc or technicalrequirement and create the Test case generation automatically for the QA team, the output should be downloadabel in .md/.txt/.csv download option should be available. 
You will be able to create a test case generation based on the by fetching the requiremnt automatically.

GROQ - openai/gpt-oss-120b (FREE) and customizable add dropdown (for now add groq, github copilet, claude antropic) groq should selected default
fETCH JIRA -> email, token, JIRA - <jira-id> or Plain Text/.txt/.md uploads (requirement or technical requiremenr doc)

